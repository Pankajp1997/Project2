#!/bin/bash 
# This is A sample project to learn the git and github 
# To initialize the git in the folder use git init 
# To Know the status of the git use git status command 
# To track the files in git use git add . --> . Will adds all the tracked files 
# To commit the files use git commit -m "Message you want to display while seeing the logs "
# To connect the folder to the github repository use git remote add origin " Url" 
# To push the tracked files into the github use git push origin master 

